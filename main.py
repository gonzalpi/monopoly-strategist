import numpy as np

# probability (P) array for a single two-dice throw where 0-th element is the probability of getting doubles
throw1 = (1/36) * np.ndarray([6, 0, 0, 2, 2, 4, 4, 6, 4, 4, 2, 2, 0])

# P array for up to 3 throws (do store P of going to jail)

# P matrix
# redistribution of P from "chance", "community chest" and "go to jail" tiles

# diagonalization of P matrix
# self-multiplying until change is minimal (must define a threshold)
# de-diagonalization

# extra: analyze profitability taking into account cost, rent and probability
